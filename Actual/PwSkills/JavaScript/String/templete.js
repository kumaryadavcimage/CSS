const obj = {
  id: 1,
  name: "iPhone",
  brand: "Apple",
  price: 200,
};

const str = `The name of the product is ${obj.name} which belongs to ${obj.brand} brand with a price ${obj.price}`; 
str
console.log(str);
